apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infrabin.fullname" . }}-test-connection"
  labels:
    {{- include "infrabin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infrabin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
